from fakepinterest import app

if __name__ == '__main__':  # e o arquivo está executando o próprio arquivo
    app.run(debug=True)  # todas as alterações são implementadas sem precisar rodar o código de novo
